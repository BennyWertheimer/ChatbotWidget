{
  "name": "outbound-chat-widget",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "ingest:sealx": "pnpm --filter @outbound/backend run ingest:sealx",
    "test:sealx": "pnpm --filter @outbound/backend run test:sealx"
  }
}